/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // distDir: 'dist',
    // basePath: '/home',
    // reactStrictMode: true,
    // images: {
    //     //loader: "custom",
    //     formats:['image/webp']
    //   },
}

module.exports = nextConfig
